# FlashDet — Pinned Dependencies
# Tested on Python 3.10 / 3.11 / 3.12 (Linux x86_64)
#
# NOTE: PyTorch is NOT listed here because CPU vs CUDA installs
#       require different index URLs. The setup_env.sh script
#       handles this automatically. If installing manually:
#
#   CPU only : pip install torch>=2.0.0 torchvision>=0.15.0
#   CUDA 11.8: pip install torch>=2.0.0 torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.x: pip install torch>=2.0.0 torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cu124

# Core (detection & training)
numpy>=1.24.0,<3.0
opencv-python>=4.8.0
Pillow>=9.0.0
pycocotools>=2.0.6
PyYAML>=6.0
tqdm>=4.65.0
termcolor>=2.0.0

# ONNX export & quantization
onnx>=1.14.0
onnxruntime>=1.15.0

# torchtune-inspired optimizations (optional — install for advanced features)
# bitsandbytes: 8-bit AdamW optimizer (--use-8bit-optimizer flag)
# bitsandbytes>=0.41.0

# Desktop UI
PyQt5>=5.15.0
matplotlib>=3.6.0
